#include "minishell.h"
//1
int		main(int argc, char **argv, char  **env)
{
	//int pid;
	//pid = 30;
	//pid = fork();
	//printf("process id = %d\n", pid);

	//pid = fork();
	////pid = fork();

	////pid = fork();
	//printf("process id = %d\n", pid);
	////fork();
	////printf("Hello world\n");
	//while (1)
	//	;
	printf("pepepo\n");
	return(0);
}